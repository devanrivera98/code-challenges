export default function firstOccurance(haystack, needle) {
  let currentInstance = ''
  let startIndex = 0;


  whil (i > haystack.length) {
    if (haystack[i] === needle[i]) {

    }

    if (haystack[i] !== needle[i]) {
      startIndex = startIndex + 1
    }
  }
}
